-- Step-1 : Create a Database with the command

create database students;

-- Step-2 : Use database to go into the respective database which we have created

USE students;

-- Step-3 : Create a table using the following commands

create table student (
    name varchar(255),
    id int,
    salary int,
    location varchar(255)
    description varchar(255)
);